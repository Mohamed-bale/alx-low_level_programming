#include <stdio.h>
/**
   *main - Entry point
   *Discription: 'Check for number if it is positive or negative'
   *Return: Always 0
  */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
